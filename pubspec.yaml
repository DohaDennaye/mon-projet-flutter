name: acces_camera
description: "A new Flutter project."

publish_to: 'none'  # Supprime cette ligne si tu veux publier sur pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.7.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.1
  permission_handler: ^12.0.0+1  # Met à jour vers la dernière version
  path_provider: ^2.1.2        # 📁 pour enregistrer la vidéo
  path: ^1.9.0
  cupertino_icons: ^1.0.8
  qr_code_scanner: ^1.0.1
  mobile_scanner: ^6.0.7
  flutter_barcode_scanner: ^2.0.0  # Met à jour vers la dernière version stable disponible
  file_picker: ^10.1.2
  image_picker: ^1.0.4
  flutter_sound: ^9.2.13

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # Exemple pour ajouter des assets si nécessaire
  # assets:
  #   - assets/images/
  #   - assets/icons/
